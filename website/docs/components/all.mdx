---
title: All components
---

import components from '../../components'

<table>
  <thead>
    <tr>
      <th>Docs</th>
      <th>Storybook</th>
      <th>Status</th>
    </tr>
  </thead>
  <tbody>
    {components
      .map(({ name, href, story, status }) => (
        <tr key={name}>
          <td><a href={href}>{name}</a></td>
          <td><StorybookLink name={name} {...story} /></td>
          <td>{status || '?'}</td>
        </tr>
      ))
    }
  </tbody>
</table>