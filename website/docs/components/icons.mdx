---
title: Icon library
see_also:
  - title: Icon usage
    href: /usage/icons/
---

import { icons } from 'sfgov-design-system/src/icons'
import { CopyToClipboard } from 'react-copy-to-clipboard'

<div className="flex flex-wrap">
  {icons.map(icon => (
    <div className="w-1/2 lg:w-1/3 mb-80">
      <div className="mx-28 no-underline text-center text-slate">
        <div className="flex justify-around mb-4">
          <sfgov-icon
            className="p-20 lg:p-60 xl:p-80 border-1 border-solid border-grey-2"
            width="28"
            height="28"
            symbol={icon.symbol}
          />
        </div>
        <CopyToClipboard
          className="cursor-pointer block relative"
          text={icon.symbol}
        >
          <button
            className="cursor-pointer btn-block group py-4 relative border-0 bg-none hocus:bg-slate-1"
            aria-label={`Copy ${icon.symbol}`}
          >
            <div className="w-full text-slate-4 font-mono pl-20">{icon.symbol}</div>
            <sfgov-icon
              symbol="clipboard"
              className="text-slate-3 invisible group-hocus:visible"
              width="20"
              height="16"
            />
          </button>
        </CopyToClipboard>
      </div>
    </div>
  ))}
</div>
